<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ackerframework.admin.center.rights.dao.RoleDao">

    <select id="getGridList" resultType="com.ackerframework.admin.center.rights.entity.EgridRole">
        SELECT id,role_name,role_code,org_id,role_state,remark,create_name,create_time,modify_name,modify_time
        <include refid="grid_list_where"></include>
    </select>
    <select id="listCount" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM sys_role WHERE 1=1
    </select>
    <sql id="grid_list_where">
        FROM sys_role WHERE 1=1 AND has_removed = FALSE
    </sql>

    <select id="get" resultType="com.ackerframework.admin.center.rights.entity.Role">
        SELECT id,role_name,role_code,org_id,role_state,remark,create_name,create_time,modify_name,modify_time
        FROM sys_role WHERE 1=1 AND id = #{id} AND has_removed = FALSE
    </select>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO sys_role (role_name,role_code,org_id,role_state,remark,create_name,create_time)
        VALUES(#{roleName},#{roleCode},#{orgId},#{roleState},#{remark},#{createName},#{createTime})
    </insert>

    <update id="update" parameterType="com.ackerframework.admin.center.rights.entity.Role">
        UPDATE sys_role
        <trim prefix="set" suffixOverrides=",">
            <if test="roleName!=null">role_name=#{roleName},</if>
            <if test="roleCode!=null">role_code=#{roleCode},</if>
            <if test="orgId!=null">org_id=#{orgId},</if>
            <if test="roleState!=null">role_state=#{roleState},</if>
            <if test="modifyName!=null">modify_name=#{modifyName},</if>
            <if test="modifyTime!=null">modify_time=#{modifyTime},</if>
        </trim>
        WHERE id = #{id}
    </update>


</mapper>